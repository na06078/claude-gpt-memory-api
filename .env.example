# Server configuration
PORT=3030

# Memory file path - Use forward slashes even on Windows or double backslashes
# Use exactly the same path as Claude's memory file - no need to copy the data
MEMORY_FILE_PATH=C:/Users/dydgu/Desktop/MCP-Tools/node_modules/@modelcontextprotocol/server-memory/dist/memory.json
