# Server configuration
PORT=3030

# Memory file path - Use forward slashes even on Windows or double backslashes
# Default location for Claude's memory - same as configured in claude_desktop_config.json
MEMORY_FILE_PATH=C:/Users/dydgu/Desktop/MCP-Tools/memory.json

# For development, you can use a local file
# MEMORY_FILE_PATH=./memory.json
